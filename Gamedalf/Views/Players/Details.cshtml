@model Player

@{
    ViewBag.Title = Model.Email;
}
@Html.Partial("_ExtendedProfile", Model)

<div class="text-right">
    <a href="@Url.Action("Index")" class="btn btn-default">Back</a>
</div>
